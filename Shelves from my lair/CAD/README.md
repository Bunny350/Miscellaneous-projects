The CAD document contains three variants of the shelves I have built, except for <s>NAS shelf</s>, which was created on its own (and it's follower-exclusive for now).

It not only include the mostly-printed filament shelf and the hybrid shelf, it also contains a three-storey rack which has its printable models not available in STL.
